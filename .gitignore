# Rust / Cargo
/target/
**/*.rs.bk
Cargo.lock.orig

# cargo-fuzz
/fuzz/target/
/fuzz/corpus/
/fuzz/artifacts/
/runtime/verification/fuzz/target/
/runtime/verification/fuzz/corpus/
/runtime/verification/fuzz/artifacts/

# Références vendored : le clone brut du dépôt mumble-voip n'est PAS commité.
# Seuls le hash pinné (references/mumble.pin) et les extraits explicitement
# vendorés (.proto, vecteurs OCB2) le sont. Voir references/README.md.
/references/mumble/

# OS / éditeurs
.DS_Store
*.swp
.idea/
.vscode/

# Java / Gradle controller integration
**/.gradle/
**/build/
