# Provided implementations

`implementations/` contains complete, opinionated voice models built on the
generic Control layers and Mumble Server Runtime. Each implementation owns its
wire payloads, host-side behavior, client SDKs, runnable composition, examples,
and implementation-specific verification.

Applications may use one of these implementations or build a different model
from `control/`. A provided implementation is not part of Coordination.
